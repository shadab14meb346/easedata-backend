export enum Interval {
  EACH_MINUTE = "EACH_MINUTE",
  EACH_HALF_HOUR = "EACH_HALF_HOUR",
  EACH_HOUR = "EACH_HOUR",
  EACH_DAY = "EACH_DAY",
  EACH_WEEK = "EACH_WEEK",
  EACH_MONTH = "EACH_MONTH",
}

export enum QueryScheduleStatus {
  ACTIVE = "ACTIVE",
  PAUSED = "PAUSED",
}
